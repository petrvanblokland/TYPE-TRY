<?xml version="1.0" encoding="utf-8"?>
<document><pre><code># ----------------------------------------
# Studies/2018-reviews.html
# ----------------------------------------
page = page.next
page.applyTemplate(template)  
page.name = 'PowerLift'
page.url = 'powerlift.html'

content = page.select('Content')
box = content.newBanner()
</code></pre>

<h1>More TYPETR PowerLift variants in the making. <a href="downloads/PowerLift_Try.zip">TRY</a> them now.</h1>
<pre><code>box = content.newCropped(fill=0.3)
</code></pre>

<p><img alt="x=center y=top contain noscale no-repeat" src="images/IMG_4664.JPG" /></p>
<h1>Hey Bun, let’s get a Croque Monsieur</h1>
<pre><code>box = content.newIntroduction() # Large color background, white large text
</code></pre>

<h1><a href="downloads/PowerLift_Try.zip">TRY</a> the heavy weight PowerLift in your design work. Two variants are released: <strong>PowerLift Tight</strong> and <strong>PowerLift Tight Outline</strong>, matching to be used in layers. A new one is to be released in 2019: <strong>PowerLift Tight Circle</strong>.</h1>
<pre><code>box = content.newTypeList(doc.docLib['fontDataList']['PowerLift'], fontSize=44, labelFont='Upgrade-Regular', labelFontSize=18)

box = content.newBanner()
</code></pre>

<h1>What designers did with the font.</h1>
<pre><code>box = content.newWaterfall(doc.docLib['fontDataList']['PowerLift'], fontSizes=[48,54,60,66,72], labelFont='Upgrade-Regular', labelFontSize=18)
</code></pre></document>