<?xml version="1.0" encoding="utf-8"?>
<document><pre><code># ----------------------------------------
# Studies/2018-reviews.html
# ----------------------------------------
page = page.next
page.applyTemplate(template)  
page.name = 'PowerLift'
page.url = 'powerlift.html'

content = page.select('Content')
box = content.newBanner()
</code></pre>

<h1>More TYPETR PowerLift variants in the making. <a href="downloads/TYPETR-PowerLift_Try.zip">TRY</a> them now.</h1>
<pre><code>box = content.newCropped(fill=0.3, cssClass='cropped cropped-second')
</code></pre>

<p><img alt="x=center y=top contain noscale no-repeat" src="images/IMG_4664.JPG" /></p>
<h1>Hey Bun, let’s get a Croque Monsieur</h1>
<pre><code>box = content.newIntroduction() # Large color background, white large text
</code></pre>

<h1><a href="downloads/TYPETR-PowerLift_Try.zip">TRY</a> the heavy weight PowerLift in your design work. Two variants are released: <strong>PowerLift Tight</strong> and <strong>PowerLift Tight Outline</strong>, matching to be used in layers. Two new variants will be released in 2019: <strong>Circle</strong> and <strong>Slab</strong>.</h1>
<pre><code>from pagebot.toolbox.units import em
box = content.newTypeList(doc.docLib['fontDataList']['PowerLift'], fontSize=120, labelFont='Upgrade-Regular', labelFontSize=18, leading=em(1), labelLeading=em(1.5))

box = content.newBanner()
</code></pre>

<h1>What designers did with the font.</h1>
<pre><code>box = content.newMain()
</code></pre>

<p><img alt="" src="images/TypeLab2019PowerLift1.jpg" /></p>
<pre><code>box = content.newSide()
</code></pre>

<p><img alt="" src="images/TypeLab2019PowerLift2.jpg" /></p>
<pre><code>box = content.newCropped(fill=0.3, cssClass='cropped cropped-third')
</code></pre>

<p><img alt="x=center y=top contain noscale no-repeat" src="images/IMG_4609.JPG" /></p>
<h1>Hey Bun, let’s get a Croque Monsieur</h1></document>