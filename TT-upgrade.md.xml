<?xml version="1.0" encoding="utf-8"?>
<document><pre><code># ----------------------------------------
# upgrade.html
# ----------------------------------------
page = page.next
page.applyTemplate(template)  
page.name = 'Upgrade'
page.url = 'upgrade.html'

content = page.select('Content')
box = content.newBanner()
</code></pre>

<h1>More TYPETR Upgrade variants in the making. <a href="downloads/Upgrade_Try.zip">TRY</a> them now.</h1>
<pre><code>box = content.newCropped(fill=0.3)
</code></pre>

<p><img alt="x=center y=top contain noscale no-repeat" src="images/IMG_4664.JPG" /></p>
<h1>You &amp; me will have a romantic dinner night.</h1>
<pre><code>box = content.newCropped(fill=0.3)
</code></pre>

<p><img alt="x=center y=top contain noscale no-repeat" src="images/IMG_4609.JPG" /></p>
<h1>Sweetheart, it’s hotdog time.</h1>
<pre><code>box = content.newIntroduction()
</code></pre>

<h1>TYPE TRY gives an updated overview of running TYPETR projects. Download sample fonts to test in your designs. Use the type, while it is still in the making. Express wishes and suggestions. Find example templates. Get code snippets that best implement TYPETR type.</h1></document>